<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.t4er.review.mapper.ReviewMapper">
	<insert id="insertReview" parameterType="Review">
		<selectKey keyProperty="rnum" order="BEFORE" resultType="int">
			select review_seq.nextval from dual
		</selectKey>
		insert into review (rnum, rstar, rrecommend, rfile1, rfile2, rfile3, rcontent, rdate, tnum, idx)
		values (#{rnum}, #{rstar}, 0, #{rfile1:VARCHAR}, #{rfile2:VARCHAR}, #{rfile3:VARCHAR}, #{rcontent}, systimestamp, #{tnum}, 0)
	</insert>
</mapper>